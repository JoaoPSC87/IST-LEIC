// expressoes variadas: ternario, base 9, reais, operadores
begin
  auto t = 0t+0-0;
  auto nove = 086;
  auto pi = 3.14;
  auto e = 1E3;
  auto b = (1 + 2) * 3 - 4 / 2 % 3;
  auto logica = 1 && 0 || ~1;
  auto cmp = 1 < 2;
  [int] vec = [5];
  auto endereco = t?;
  auto tam = sizeof(pi);
  vec[0] = 42;
  return 0;
end
